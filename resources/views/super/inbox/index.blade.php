@extends('layouts.main')
@section('title', 'Inbound Email Inbox')
@section('content')
<div class="container-fluid px-4 py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><h3 class="fw-bold mb-1">Inbound Email Inbox</h3><p class="text-muted mb-0">Manage messages sent to school and company email addresses.</p></div><form><select name="mailbox" class="form-select" onchange="this.form.submit()"><option value="">All mailboxes</option><option value="school" @selected(request('mailbox') === 'school')>School mailboxes</option><option value="main" @selected(request('mailbox') === 'main')>Company main email</option></select></form></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>School / Mailbox</th><th>From</th><th>Subject</th><th>Status</th><th>Received</th><th></th></tr></thead><tbody>
@forelse($messages as $message)<tr class="{{ !$message->is_read ? 'fw-bold' : '' }}"><td>{{ $message->school?->name ?: 'Company main email' }}<small class="d-block text-muted">{{ $message->recipient_email }}</small></td><td>{{ $message->sender_name ?: $message->sender_email }}<small class="d-block text-muted">{{ $message->sender_email }}</small></td><td>{{ $message->subject ?: '(No subject)' }}</td><td><span class="badge text-bg-light">{{ ucfirst($message->status) }}</span></td><td>{{ optional($message->received_at)->format('d M Y, h:i A') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.inbox.show', $message->id) }}">Open</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No incoming emails yet.</td></tr>@endforelse
</tbody></table></div><div class="p-3">{{ $messages->links() }}</div></div></div>
@endsection
